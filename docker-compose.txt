version: '3'

services:
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: ex12345
    ports:
    - 27018:27017
    volumes:
    - './mongo:/data/db'

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: server
    restart: unless-stopped
    env_file:
      - ./server/.env
    ports:
      # - 3001:3001
      - 3001:3001
    volumes:
      - './client/static/avatars/:/server/uploads/avatars/'
    working_dir: /server

  client:
    build: 
      context: ./client
      dockerfile: DockerFile
    container_name: client
    restart: always
    ports:
      - 3000:3000
    volumes:
      - './client/static/avatars/:/server/uploads/avatars/'
    environment:
      HOST: 0.0.0.0

networks:
  default:
    name: nmo
    external: true
